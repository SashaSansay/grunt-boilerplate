// Set your own font-size using the fontRatio variable.
// When using fontRatio, increase the value to make the font smaller (and vice verse).
$('html').flowtype({
    fontRatio : 30,
    minFont : 10,
    maxFont : 16
});
